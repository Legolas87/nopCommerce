@model ReturnRequestModel

@{
    //page title
    ViewBag.PageTitle = (await T("Admin.ReturnRequests.EditReturnRequestDetails")).Value;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Return requests");
}

<form asp-controller="ReturnRequest" asp-action="Edit" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @await T("Admin.ReturnRequests.EditReturnRequestDetails")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@await T("Admin.ReturnRequests.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="notify-customer" class="btn bg-purple">
                <i class="fas fa-at"></i>
                @await T("Admin.ReturnRequests.NotifyCustomer")
            </button>
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-save"></i>
                @await T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-save"></i>
                @await T("Admin.Common.SaveContinue")
            </button>
            <span id="returnrequest-delete" class="btn btn-danger">
                <i class="far fa-trash-alt"></i>
                @await T("Admin.Common.Delete")
            </span>
            @await Component.InvokeAsync(typeof(AdminWidgetViewComponent), new { widgetZone = AdminWidgetZones.ReturnRequestDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="returnrequest-delete" />